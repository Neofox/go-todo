package components

type ButtonProps struct {
	Label string
}

templ Button(props ButtonProps) {
	<button class="bg-blue-500 text-white px-4 py-2 rounded-md border border-blue-500 hover:bg-blue-400">
		{ props.Label }
	</button>
}
